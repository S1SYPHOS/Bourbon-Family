{
  "name": "Bourbon-Family",
  "version": "1.0.0",
  "description": "Sass boilerplate starring Bourbon, Neat & Bitters, mixed up with some Normalize.css - ready to be served!",
  "main": "src/main.scss",
  "repository": "https://github.com/S1SYPHOS/Bourbon-Family.git",
  "author": "S1SYPHOS <hello@twobrain.io>",
  "license": "MIT",
  "scripts": {
    "setup": "npm install && bundle install --binstubs",
    "node": "node-sass src/main.scss -o dist/",
    "ruby": "sass --sourcemap=none src/main.scss dist/main_ruby.css",
    "test": "rimraf dist && npm run node && npm run ruby",
    "update": "bundle update && ./update.sh"
  },
  "devDependencies": {
    "node-sass": "^4.5.2",
    "rimraf": "^2.6.1"
  }
}
