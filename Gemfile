source 'https://rubygems.org'

gem 'sass' # dependency of the whole bourbon family, so it's just for fun

group :bourbon_family do
  gem 'bourbon' # dependency of bitters, so it's just for fun
  gem 'neat'
  gem 'bitters'
end
